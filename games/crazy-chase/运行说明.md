# 《疯狂追追追》运行说明

不要直接双击 `index.html` 打开。

请双击：

`start-crazy-chase.command`

它会自动启动本地服务，并打开：

`http://localhost:5173/`

如果系统提示无法打开 `.command` 文件，可以右键选择“打开”。

为什么不能直接打开 `index.html`？

因为游戏里的角色动作表需要在浏览器里做透明背景处理。直接用本地文件方式打开时，浏览器安全限制可能会阻止图片处理，角色就会退回成简笔占位图。

正确运行方式必须是本地服务器地址：

`http://localhost:5173/`
